spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=testdb

#adding logging to path
logging.file.path=D:/Projects/Personal Projects/REST-Spring-Boot
# server.servlet.context-path : /demo => this tells every rest end point should be /demo, E.g. /demo/greeting

# we can expose all the actuator end points for our app monitoring
management.endpoints.web.exposure.include=*

# we can also shutdown out application using 
mamagement.endpoint.shutdown.enabled=true


